import { SportsPortalLayout } from '@/components/SportsPortalLayout';

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return <SportsPortalLayout>{children}</SportsPortalLayout>;
}
